Tablas disponibles para analisis demo:

1) customers
- id (PK)
- customer_code
- full_name
- gender
- age
- birth_date
- email
- phone
- city
- state
- segment
- monthly_income
- risk_profile
- created_at

2) vehicles
- id (PK)
- customer_id (FK -> customers.id)
- vin
- plate
- brand
- model
- unit_type
- year
- fuel_type
- mileage
- created_at

3) sales
- id (PK)
- customer_id (FK -> customers.id)
- vehicle_id (FK -> vehicles.id)
- sale_date
- amount
- channel
- seller
- payment_method
- status

4) services  ← servicios realizados (tiene cost y service_date)
- id (PK)
- customer_id (FK -> customers.id)
- vehicle_id (FK -> vehicles.id)
- service_date  ← fecha del servicio (solo existe aquí, NO en service_appointments)
- service_type
- cost  ← monto del servicio (solo existe aquí, NO en service_appointments)
- status
- workshop
- notes

5) service_appointments  ← agenda de citas (NO tiene cost, notes ni service_date)
- id (PK)
- customer_id (FK -> customers.id)
- vehicle_id (FK -> vehicles.id)
- appointment_date  ← fecha de la cita (NO existe service_date aquí)
- service_type
- appointment_status (completada, programada, no_show, cancelada)  ← NO se llama status
- workshop
- cancellation_reason
- attended
- created_at
- updated_at

NOTA: services y service_appointments son tablas diferentes.
- Para costos de servicio → usa services.cost
- Para fechas de servicio → usa services.service_date
- Para agenda/citas → usa service_appointments con appointment_date y appointment_status

6) insurance_policies
- id (PK)
- customer_id (FK -> customers.id)
- vehicle_id (FK -> vehicles.id)
- policy_start_date
- policy_end_date
- insurer
- coverage_type
- annual_premium
- policy_status
- claim_count
- last_claim_date

7) mv_sales_monthly (tabla materializada para analytics)
- year_month
- state
- channel
- segment
- total_sales
- sales_count

8) mv_customer_lifecycle (tabla materializada para analytics)
- customer_id
- purchases
- total_amount
- first_sale_date
- last_sale_date
- avg_repurchase_days

Relaciones clave:
- customers.id -> vehicles.customer_id
- customers.id -> sales.customer_id
- customers.id -> services.customer_id
- customers.id -> service_appointments.customer_id
- customers.id -> insurance_policies.customer_id
- vehicles.id -> sales.vehicle_id
- vehicles.id -> services.vehicle_id
- vehicles.id -> service_appointments.vehicle_id
- vehicles.id -> insurance_policies.vehicle_id
- customers.id -> mv_customer_lifecycle.customer_id

Preguntas de negocio que soporta este demo:
- Citas agendadas de servicio por estatus (completada/programada/no_show/cancelada).
- Tasa de no show por estado, canal o taller.
- Citas programadas para los próximos 7/30 días.
- Top no show por taller.
- Cancelaciones por motivo y por taller.
- Conversión de citas programadas a completadas por mes.
- Tiempo promedio de recompra por cliente (diferencia entre compras consecutivas en sales.sale_date).
- Edad promedio de compradores y analisis por gender.
- Tipo de unidad recomendado por rango de edad y genero (vehicles.unit_type + customers.age/gender).
- Elegibilidad para ofertar seguro:
  - clientes con vehiculo y sin poliza activa,
  - o poliza activa por vencer (policy_end_date cercano),
  - o poliza vencida.

Buenas practicas:
- Cualquier listado o detalle por vehículo debe incluir siempre la columna vin (tabla vehicles); si solo hay vehicle_id, JOIN vehicles y selecciona vin.
- Para recompra, usar ventanas (LAG) en sales por customer_id y calcular dias entre compras.
- Para recomendacion por rango de edad, agrupar por buckets de age y unit_type.
- Para seguro, combinar customers + vehicles + insurance_policies filtrando por policy_status y fechas.
- Para agenda de servicio, usar service_appointments y filtrar por appointment_status/appointment_date.
- Para pronósticos y tendencias mensuales, preferir mv_sales_monthly en lugar de recalcular desde sales.
- Para recompra y ciclo de vida por cliente, preferir mv_customer_lifecycle.
